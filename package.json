{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build && rm -rf docs/* && cp -r build/* docs",
    "publish": "yarn run build && git add -A && git commit -m 'Build site' && git push",
    "test": "jest",
    "format": "prettier --write \"src/**/*.{js,jsx,css}\"",
    "lint": "prettier --check \"src/**/*.{js,jsx,css}\""
  },
  "dependencies": {
    "@babel/plugin-syntax-class-properties": "^7.10.4",
    "react": "^16.13.1",
    "react-beautiful-dnd": "^13.0.0",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "@babel/plugin-proposal-class-properties": "^7.10.4",
    "@snowpack/app-scripts-react": "^1.10.0",
    "@snowpack/plugin-webpack": "^2.0.12",
    "@testing-library/jest-dom": "^5.5.0",
    "@testing-library/react": "^10.0.3",
    "jest": "^26.2.2",
    "prettier": "^2.0.5",
    "snowpack": "^2.11.0"
  }
}
