{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build && rm -rf docs && mkdir docs && cp -r build/* docs",
    "publish": "yarn run build && git add -A && git commit -m 'Build site' && git push",
    "test": "jest",
    "format": "prettier --write \"src/**/*.{js,jsx,css}\"",
    "lint": "prettier --check \"src/**/*.{js,jsx,css}\""
  },
  "dependencies": {
    "@babel/plugin-syntax-class-properties": "^7.12.1",
    "react": "^17.0.1",
    "react-beautiful-dnd": "^13.0.0",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@babel/plugin-proposal-class-properties": "^7.12.1",
    "@snowpack/app-scripts-react": "^1.12.6",
    "@snowpack/plugin-webpack": "^2.1.0",
    "prettier": "^2.1.2",
    "snowpack": "^2.16.1"
  }
}
